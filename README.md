- put files into ./file directory

commands to start thing
- ./mvnw clean package
- ./mvnw exec:java -Dexec.mainClass="Main" -Dexec.args="3.uai 3.uai.evid"

partition function would be outputted in log base 10 format